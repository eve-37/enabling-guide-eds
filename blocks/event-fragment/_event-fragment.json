{
  "definitions": [
    {
      "title": "Event Fragment",
      "id": "event-fragment",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Event Fragment",
              "model": "event-fragment",
              "fragmentPath": "",
              "source": "servlet"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "event-fragment",
      "fields": [
        {
          "component": "aem-content",
          "name": "fragmentPath",
          "label": "Content Fragment",
          "description": "The event Content Fragment to render. Leave empty to use the fragment named in this page's properties.",
          "valueType": "string",
          "rootPath": "/content/dam/enabling-guide-eds"
        },
        {
          "component": "select",
          "name": "source",
          "label": "Data source",
          "description": "Where the event data is read from. Both render identically; this exists to compare them. GraphQL requires a Content Fragment above - it cannot resolve one from the page.",
          "valueType": "string",
          "value": "servlet",
          "options": [
            { "name": "Servlet (AEM JSON endpoint)", "value": "servlet" },
            { "name": "GraphQL (persisted query)", "value": "graphql" }
          ]
        }
      ]
    }
  ],
  "filters": []
}
